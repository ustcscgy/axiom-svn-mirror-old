
pamphlets = Makefile.pamphlet

subdir = src/

SUBDIRS = @axiom_src_subdirs@

.PHONY: all all-ax all-src all-clef all-sman 

all: all-ax

all-ax all-src: stamp
	@echo finished $(builddir)

stamp: @axiom_src_all@
	-rm -f stamp
	$(STAMP) stamp

all-clef: all-lib
	@$(mkinstalldirs) $(axiom_target_bindir)
	cd clef && ${MAKE} $(AX_FLAGS)
all-sman: all-lib
	$(mkinstalldirs) $(axiom_target_bindir)
	$(mkinstalldirs) $(axiom_target_libdir)
	cd sman && ${MAKE} $(AX_FLAGS)
all-hyper: all-lib
	$(mkinstalldirs) $(axiom_target_bindir)
	$(mkinstalldirs) $(axiom_target_datadir)
	cd hyper && ${MAKE} $(AX_FLAGS)
all-share:
	$(mkinstalldirs) $(axiom_target_libdir)
	cd share && ${MAKE} $(AX_FLAGS)
all-doc:
	@ $(mkinstalldirs) ./$(top_builddir)/int/doc
	cd doc && ${MAKE} $(AX_FLAGS)
all-book:
	cd booklets && ${MAKE} $(AX_FLAGS)
all-lib:
	$(mkinstalldirs) $(OUT)
	@cd lib && ${MAKE} $(AX_FLAGS)
all-lisp: all-lib all-boot
	$(mkinstalldirs) $(axiom_build_bindir)
	cd lisp && $(MAKE) $(AX_FLAGS) all-lisp
all-boot:
	$(mkinstalldirs) $(axiom_build_bindir)
	@cd boot && ${MAKE} $(AX_FLAGS)
all-depsys: all-boot
	cd interp && $(MAKE) $(AX_FLAGS) all-depsys

all-interpsys: all-depsys all-lisp
	cd interp && ${MAKE} $(AX_FLAGS) all-interpsys

all-axiomsys: all-asq
	cd interp && $(MAKE) $(AX_FLAGS) all-axiomsys
all-algebra: all-interpsys all-share
	@ $(mkinstalldirs) algebra/strap
	cd algebra && ${MAKE} $(AX_FLAGS)
all-input: all-axiomsys
	cd input && ${MAKE} $(AX_FLAGS)
all-asq: all-algebra
	cd etc && ${MAKE} $(AX_FLAGS)
all-graph: all-lib
	@ $(mkinstalldirs) ./$(top_builddir)/int/graph/parabola
	cd graph && ${MAKE} $(AX_FLAGS)

mostlyclean-local:
	-rm -f stamp

clean-local: mostlyclean-local

distclean-local: clean-local

