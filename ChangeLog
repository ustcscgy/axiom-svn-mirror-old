2006-09-14  Ralf Hemmecke  <ralf@hemmecke.de>

	* src/doc/french/ac20.pdf, src/doc/french/manuel1.pdf,
	src/doc/french/manuel2.pdf, src/doc/bookvol1.pdf,
	src/share/algebra/libaxiom.al, zips/tla-1.1.tar.gz, *.Z:
	Corrected corrupted binary files by taking the corresponding files
	from axiom--main--1--patch-50.
	cd $Gold
	find . -type f ! -regex '.*\.arch-ids.*' ! -regex '.*{arch}.*' \
	     ! -regex '.*\.patch' ! -exec cmp {} $Silver/trunk/axiom/{} ';' \
	     -exec cp {} $Silver/trunk/axiom/{} ';'

2006-09-13  Ralf Hemmecke  <ralf@hemmecke.de>
	
	* All files:
	svn propdel svn:keywords -R
	Removed svn:eol-style property.
	find -type f -name '*.Z' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.al' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.tar' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.gz' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.KAF' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.daase' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.data' -exec svn propdel svn:eol-style {} ';'
	find -type f -name '*.db' -exec svn propdel svn:eol-style {} ';'
	find -type f -regex '.*\.\(eps\|epsi\|ps\)$' -exec svn propdel svn:eol-style {} ';'
	find -type f -regex '.*\.\(ht\|pht\|list\|mask\|msgs\|out\|tiny\|tm\|wrong\|xbm\)$' -exec svn propdel svn:eol-style {} ';'
	find -type f -regex '.*\.\(bitmap\|bakmap\|bm\)$' -exec svn propdel svn:eol-style {} ';'
	find -type f -regex '.*\/\(bitmap\|pixmap\|data\|door\|graph0\|graph1\|ht_icon\)$' -exec svn propdel svn:eol-style {} ';'

2006-08-21  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-07-31  Ralf Hemmecke  <ralf@hemmecke.de>

	* Makefile.pamphlet, src/scripts/Makefile.pamphlet,
	src/doc/Makefile.pamphlet: After checking out the trunk of Axiom
	Silver, a 'make' did not succeed because the src/script directory
	was copied twice. Since the .svn directory inside contains
	write-protected files, it caused problems.

	Quote from Makefile.pamplet:

	Since this is the first build message that gets generated
	we echo the ENV variable for debugging purposes. We use the
	specific file[[${MNT}/${SYS}/bin/Makefile.pamphlet]] as the
	trigger to prevent duplicate execution of this stanza.

	That is now also used in src/scripts/Makefile.pamphlet.

2006-07-31  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-07-30  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-07-15  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-06-17  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-06-11  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

	* Makefile (GCLVERSION): Sync with Makefile.pamphlet

2006-06-05  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-05-31  Gabriel Dos Reis  <gdr@cs.tamu.edu>

	* Makefile.pamphlet (VERSION): Update.

2006-05-16  Gabriel Dos Reis  <gdr@acm.org>

	* Makefile.pamphlet (VERSION): Update.

2006-05-02  Gabriel Dos Reis  <gdr@acm.org>

	* CHANGELOG: Sync with gold branch.
	* FAQ: Likewise.
	* Makefile: Likewise.
	* Makefile.pamphlet: Likewise.
	* README: Likewise.

2006-05-02  Gabriel Dos Reis  <gdr@acm.org>

	* axiomlogo.jpg: Fix corruption inherited from the conversion to
	SVN repo.
	* axiom30yr.jpg: Likewise.
	* axiomsml.png: Likewise.
	* axiomicon.png: Likewise.

